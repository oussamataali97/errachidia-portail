   <div class="col-lg-9 ">
           <div class="row g-4">
               <h2 class="text-orange mb-4">Actualités</h2>

   {% for row in rows %}
  

 <div class="col-md-4 card-annoncess">
            <div class="card-shadow">
                <img class="img-annonces" src=" {{file_url(row.content['#row']._entity.field_image_actualite.entity.uri.value)}}" class="card-img-top" alt="Image 1">
              <div class="card-body">
                <h5 class="card-title"> {{row.content['#row']._entity.field_libelle[0].value|slice(0, 50)}}  </h5>
                <p class="card-date">{{ row.content['#row']._entity.created.value|date("d F Y") }}
</p>
                <p class="card-text annonce-desciptions">
{{ row.content['#row']._entity.field_description[0].processed|striptags|replace({'&nbsp;': ' '})|slice(0, 50) }}
                </p>
                <a href="{{ url('entity.node.canonical', {'node': row.content['#row']._entity.nid.value}) }}" class="annonce-link">Lire la suite</a>
              </div>
            </div>
         
            </div>
        
{% endfor %}
       <div class="mt-4 text-center">
          <a href="{{ base_path }}/portail/actualite"><button class="btn btn-primary"><i class="fas fa-eye " style="font-size:12px;"></i> Voir plus</button></a>
        </div>
  </div>
</div>





